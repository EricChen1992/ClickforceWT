<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.click.force.clickforce.working.time">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" android:maxSdkVersion="22" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <!--        android:networkSecurityConfig="@xml/network_security_config"-->
    <!-- 讀取網路狀態 -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_clickforce"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_clickforce_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        >
        <activity android:name=".WelcomeActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".WorkActivity"/>
        <meta-data
            android:name="com.google.android.actions"
            android:resource="@anim/welcome_anim" />
    </application>

</manifest>